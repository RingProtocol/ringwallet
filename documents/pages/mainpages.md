Please check below as page/tab architecture:

1. Bottom multiple tabs must dock at bottom
2. if content for every tab is very high, tabs must visble, just make content scrollable.
3. Top navigation bar and bottom tabs are in fixed position, never moved.
4. content of More tab and Activity tab must have top margin from the bottom of 'Navigation bar' .
5. Encapsue a gradient style card as common part in every tab, they have the same height which equals to distance from device top to the bottom of transactionAction component.
