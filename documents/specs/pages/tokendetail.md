# entrance

- click one token from 'token list'

# what to show

## from top to bottom:

- back arrow in token detail's navigation bar.(click to quit detail page)
- token name
- token price( change percentage)
- Actionbar: Swap/Send (Send is to open SendForm, Swap is to open RingSwap dapp)
- balance:
  logo + 5 token symbol
- detail:
  network:
  contract address:
  decimals:
  approved amount:
  xx "edit" button
  Activity:
  transaction list.
