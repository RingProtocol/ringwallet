1. 在@src/models 定义钱包类型的枚举值：钱包类型分智能合约和EOA两种。然后替换到@src/components/LoginButton.tsx:54-55 ；
2. 默认支持EOA钱包；智能合约先不支持。到时在Account抽屉里面弄个升级到智能合约钱包，再把类型变成智能合约类型；
3. 
7951的能力应该是secp256r1签名方式，在设置项里面勾选了才算用它来签名。而且这种签名方式只有以太坊支持。L2估计都不支持。
4. 在签名时再次弹出人脸识别，通过后再签名。